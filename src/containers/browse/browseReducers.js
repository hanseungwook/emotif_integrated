// import {} from './browseActions';

const initialState = {
  isAuthenticated : false
};
const browseReducer = (state = initialState, action) => {
  switch (action.type) {
    case 0:
      return state;
    default:
      return state;
  }
};

export default browseReducer;
